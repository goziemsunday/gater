info:
  name: become-organizer
  type: http
  seq: 7

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/become-organizer"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
