info:
  name: get-user
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/api/auth/me"
  body:
    type: json
    data: |
      {
        "name": "Chiagoziem Sunday",
        "email": "gozmansunday@gmail.com",
        "password": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
