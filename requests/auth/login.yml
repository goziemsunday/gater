info:
  name: login
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/api/auth/login"
  body:
    type: json
    data: |-
      {
        "email": "gozmansunday@gmail.com",
        "password": "12345678"
        
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
